<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yunjian">
    <!-- 网络访问权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 摄像头调用权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--        android:networkSecurityConfig="@xml/network_security_config"-->
    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".XunjianItemActivity"></activity>
        <activity android:name=".First_Notification" />
        <activity android:name=".First_Resource" />
        <activity android:name=".First_LogInfomation" />
        <activity android:name=".First_Xunjian" />
        <activity
            android:name=".LunchActivity"
            android:theme="@style/LunchStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Mine_Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>