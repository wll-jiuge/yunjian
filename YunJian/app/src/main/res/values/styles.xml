<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="XUITheme.Phone">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
<!--    启动页样式-->
    <style name="LunchStyle" parent="Theme.AppCompat">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/im_lunchimg</item>
    </style>

    <style name="edittextStyle">
        <item name="colorControlNormal">@color/edtext_normal</item>
        <item name="colorControlActivated">@color/toolbar_bgcolor</item>
    </style>
</resources>