<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:id="@+id/minelist_layout_myteam"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_minelist_myteam"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/toolbar_bgcolor"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme">

        <TextView
            android:id="@+id/txt_toolbar_minelist_myteam"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_color"
            android:text="@string/minelist_team" />

    </androidx.appcompat.widget.Toolbar>
    <ListView
        android:id="@+id/listview_minelist_myteam"
        android:layout_width="match_parent"
        android:divider="@color/edtext_normal"
        android:layout_height="match_parent" />
</LinearLayout>